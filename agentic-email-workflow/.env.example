# OpenAI API Key for the agents
OPENAI_API_KEY=your_openai_api_key_here

# Google OAuth credentials are loaded from token.json file
# Make sure token.json is in the root directory with valid OAuth tokens